# Cloudflare Worker config for the live-scores proxy.
# Deploy:  cd workers && npx wrangler deploy --config wrangler.live.toml
# (requires `wrangler login` or CLOUDFLARE_API_TOKEN for your account)
name = "aimatchlab-live"
main = "live-worker.js"
compatibility_date = "2025-01-01"
workers_dev = true
